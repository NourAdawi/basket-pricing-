
1- create penceToPounds and poundsToPence methods in @src/money.ts that converts pence prices to pounds and vice versa

2- Create a test suite inside @tests/pricer.test.ts  for the priceBasket function, it calculates the subtotal for now, no offers logic yet. test cases are:
 1- prices an empty basket as zero across the board
 2- prices a single item
 3- multiplies by quantity
 4- treats a zero quantity as contributing nothing
 5- throws UnknownProductError for a basket item not in the catalogue
 6- does not fall victim to float drift: 0.10 + 0.20  !== 0.30 in float arithmetic


3- create a new factory in src/catalogue.ts, catalogueFromPounds, build a  Catalogue from prices in pounds, prices are converted to integer pence once here, so malformed prices fail at construction rather than mid ttransaction. consumers of this
library work in pounds; pence is an internal representation.
after that update the setup block in test to use catalogueFromPounds. and create a simple test in catalogue.test.ts that tests malformed prices, it should failed at construction. catalogueFromPounds({ "milk": NaN })).toThrow();
export new factory from src/index.ts

4- create a test suite for money.ts utils that test the conversion + malformed and rejected values + round tripping 

5- Write three tests for these examples in a new acceptence.tests.ts, even though they are failing for now 

6- update money.tests.ts to cover roundHalfUp

7- write the percentageOff factory funtion, validate the discount first; misconfiguration fails at construction. the computeDiscpunt method returns 0 discount in two cases:
offered product is not the basket + product is no longer in the catalouge. Rounding is happing on the total, not on each unit. 
create a new test suite for the percentageOff factory.

8- Following percentageOff offer and tests example, write a test suite for mulit-buy offer 

9- Inside cheapestFreeInSet factory, write the computeDiscount logic as following:
   1- Flatten the basket into indiviual prices
   2- Group the products and see how many items go free
   3- Make the free ones as expensive as possible in each group, this is done by ordering products descending, most expensive first; the best
    available first free item is the groupSize-th

10- Write end-to-end test suite while the library is driven through its public entry point, it should demonstrate how a web shop or a till would use it.
   test cases are: a basket with every kind of offer + several offers on the same product + basket should never results in negative price + cases that's caused because catalouge and offers are maintained by different teams; discountiued product and items with no offers

11- Format the basket and the output of the priceBasket in a receipt style
 